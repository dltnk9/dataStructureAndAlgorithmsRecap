# 집합(Set)
#
python: set
a = set()  # a={}
b = {3, 5, 1, 3}  # b={3,5,1}

# 1. make-set(x) {x}
# 2. find(x) : membershipftn. x가 속한 집합을 리턴, (집합의 대푯값을)
# 3. union(x,y) 둘다 키값이다 union 해준다

# Graph 자료구조 - 가장 복잡한(일반적인)자료구조
# G=(V,E) V- vertex set, E - edge set

# Graph 표현법 ->인접성 1.인접행렬 adjacency matrix, 2.인접리스트 - adjacency list 연결리스트
# undirected graph, directed graph
